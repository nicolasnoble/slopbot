DISCORD_TOKEN=your-discord-bot-token

# Option A: Anthropic API key (from console.anthropic.com)
# ANTHROPIC_API_KEY=sk-ant-...

# Option B: Leave ANTHROPIC_API_KEY unset and run `claude login` first
# to authenticate with a Claude.ai account

# Multi-channel mapping: comma-separated channel:path pairs.
# Each channel gets its own working directory for Claude sessions.
# CHANNELS=claude:/home/pixel/proj1,dev:/home/pixel/proj2

# Legacy single-channel config (used as fallback when CHANNELS is not set):
WATCH_CHANNEL=claude
# CLAUDE_CWD=/path/to/project

# Claude model to use. Default: claude-sonnet-4-5-20250929
# CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Rate limit for streaming edits in ms. Default: 1500
# EDIT_RATE_MS=1500

# Permission mode. Default: bypassPermissions
# PERMISSION_MODE=bypassPermissions

# Session inactivity timeout in minutes. Default: 60
# SESSION_TIMEOUT_MINUTES=60

# Path to the Claude config directory (credentials, plans, etc.). Default: ~/.claude
# Useful for running multiple bot instances with different Claude accounts.
# CLAUDE_CONFIG_DIR=/home/pixel/.claude-work

# Enable verbose debug logging. Default: false
# DEBUG=true
